apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubernetes-dashboard
  namespace: kube-system
spec:
  rules:
    - host: example.dev
      http:
        paths:
          - path: /
            pathType: Exact
            backend:
              service:
                name:  kubernetes-dashboard
                port:  
                  number: 443